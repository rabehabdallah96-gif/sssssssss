🌟 SentraOS: منصة المراقبة والأمان الذكية
<!-- مشروع تخرج متكامل للمراقبة الأمنية وإدارة البنية التحتية -->
<div dir="rtl">
📚 معلومات المشروع
الطالب: محمد أحمد
الجامعة: جامعة القاهرة
القسم: علوم الحاسب
رقم المشروع: GPJ2517
السنة الأكاديمية: 2024/2025

📖 نظرة عامة على المشروع
SentraOS هي منصة شاملة ومتكاملة للمراقبة الأمنية وإدارة العمليات مبنية بلغة Python. يوفر النظام مراقبة فورية للبنية التحتية للخوادم، فحص أمني تلقائي، تنبيهات ذكية، ولوحة تحكم ويب حديثة للعرض والإدارة.
🎯 الأهداف الرئيسية

المراقبة الفورية: متابعة أداء النظام لحظة بلحظة
الفحص الأمني: كشف الثغرات والمنافذ المفتوحة
تحليل الباكتات: مراقبة وتحليل حركة الشبكة
محاكاة الشبكات: بناء واختبار توبولوجيا الشبكات
مراقبة السحابة: متابعة حاويات Docker والموارد
تكامل GNS3: إدارة ومراقبة مشاريع GNS3


✨ المميزات الرئيسية
1. 🖥️ مراقبة النظام الفورية

مراقبة المعالج (CPU): تتبع استخدام وحمل المعالج
مراقبة الذاكرة (RAM): متابعة استخدام وتوفر الذاكرة
مراقبة القرص: تتبع استخدام التخزين
مقاييس الشبكة: مراقبة النطاق الترددي وحركة المرور

2. 🔒 الفحص الأمني

فحص المنافذ: كشف المنافذ المفتوحة باستخدام nmap
كشف الثغرات: تحديد المخاطر الأمنية الشائعة
تقييم المخاطر: حساب تلقائي لمستوى الخطورة
كشف الخدمات: تحديد الخدمات النشطة وإصداراتها

3. 🔔 نظام التنبيهات الذكي

تنبيهات الأداء: إشعارات تجاوز عتبات المعالج والذاكرة
تنبيهات الأمان: إخطارات الثغرات والتهديدات
مستويات الخطورة: تصنيف منخفض، متوسط، عالي، وحرج
إدارة التنبيهات: تتبع ومعالجة دورة حياة التنبيهات

4. 📦 تحليل الباكتات (Packet Analyzer)

التقاط الحزم: مراقبة حركة الشبكة الفورية
تحليل البروتوكولات: TCP, UDP, ICMP, ARP, DNS
كشف التهديدات: اكتشاف Port Scanning و DNS Tunneling
إحصائيات مفصلة: توزيع البروتوكولات وأكثر IPs نشاطاً
📍 الملف: packet_analyzer/analyzer.py

5. 🌐 محاكي توبولوجيا الشبكات (Network Topology)

بناء الشبكات: إنشاء شبكات افتراضية كاملة
اختبار Ping: فحص الاتصال بين الأجهزة
Traceroute: تتبع مسار البيانات
جداول التوجيه: عرض وإدارة جداول التوجيه
تصور تفاعلي: رسم بياني للشبكة باستخدام Vis.js
📍 الملف: network_topology/simulator.py

6. ☁️ مراقبة الخدمات السحابية (Cloud Monitor)

مراقبة Docker: متابعة حاويات Docker والموارد
الصور والشبكات: إدارة Docker images و networks
إحصائيات الموارد: CPU, Memory, Network لكل حاوية
تقدير التكلفة: حساب التكلفة التقديرية للموارد
📍 الملف: cloud_monitor/monitor.py

7. 🎮 تكامل GNS3 Network Simulator

إدارة المشاريع: عرض وإدارة مشاريع GNS3
مراقبة الأجهزة: متابعة حالة Routers, Switches, PCs
التحكم الكامل: تشغيل وإيقاف جميع الأجهزة
تصور الشبكة: عرض توبولوجيا GNS3 بشكل تفاعلي
دعم المصادقة: اتصال آمن مع GNS3 Server
📍 الملف: gns3_integration/monitor.py

8. 🔐 نظام المصادقة والمستخدمين

تسجيل الدخول: نظام مصادقة آمن
إنشاء الحسابات: تسجيل مستخدمين جدد
إدارة الجلسات: حماية الصفحات والـ APIs
سجل النشاطات: تتبع جميع العمليات
📍 الملف: dashboard/auth.py

9. 🤖 المهام التلقائية

المراقبة المجدولة: فحص النظام كل 30 ثانية
الفحوصات الدورية: فحوصات أمنية كل 5 دقائق
الاستجابة التلقائية: إجراءات تلقائية حسب الخطورة
📍 الملف: automation/auto_responder.py


🛠️ التقنيات المستخدمة
تقنيات Backend
التقنيةالإصدارالاستخدامPython3.11+لغة البرمجة الأساسيةFlask3.1.2إطار عمل الويبSQLAlchemy2.0.44ORM لقاعدة البياناتpsutil7.1.0مراقبة النظامpython-nmap0.7.1الفحص الأمنيScapy2.5.0تحليل الباكتاتAPScheduler3.11.0جدولة المهامRequests2.31.0اتصالات HTTP
تقنيات Frontend

Bootstrap 5: تصميم واجهة متجاوب
Chart.js: رسوم بيانية تفاعلية
Vis.js Network: تصور توبولوجيا الشبكات
Font Awesome 6: مكتبة الأيقونات
Vanilla JavaScript: تحديثات فورية

قاعدة البيانات

SQLite: قاعدة بيانات خفيفة
الجداول:

users: بيانات المستخدمين
system_metrics: مقاييس النظام
scan_results: نتائج الفحوصات
alerts: التنبيهات
packet_captures: بيانات التقاط الباكتات
activity_logs: سجل النشاطات




📁 هيكل المشروع
SentraOS/
│
├── main.py                          # نقطة بدء التطبيق الرئيسية
├── models.py                        # نماذج قاعدة البيانات
├── requirements.txt                 # التبعيات المطلوبة
├── .env                            # إعدادات البيئة
├── .env.example                    # ملف بيئة نموذجي
├── README.md                       # وثائق المشروع (هذا الملف)
├── INSTALLATION.md                 # دليل التثبيت التفصيلي
├── sentra.db                       # قاعدة البيانات SQLite
│
├── network_monitor/                # وحدة مراقبة النظام
│   ├── __init__.py
│   └── monitor.py                  # جمع مقاييس النظام
│
├── security_scanner/               # وحدة الفحص الأمني
│   ├── __init__.py
│   └── scanner.py                  # فحص المنافذ والثغرات
│
├── packet_analyzer/                # ✨ وحدة تحليل الباكتات
│   ├── __init__.py
│   └── analyzer.py                 # التقاط وتحليل حزم الشبكة
│
├── network_topology/               # ✨ وحدة محاكاة الشبكات
│   ├── __init__.py
│   └── simulator.py                # بناء واختبار الشبكات
│
├── cloud_monitor/                  # ✨ وحدة مراقبة السحابة
│   ├── __init__.py
│   └── monitor.py                  # مراقبة Docker والموارد
│
├── gns3_integration/               # ✨ وحدة تكامل GNS3
│   ├── __init__.py
│   └── monitor.py                  # إدارة ومراقبة GNS3
│
├── docker_monitor/                 # وحدة مراقبة Docker
│   ├── __init__.py
│   └── monitor.py                  # إحصائيات Docker
│
├── automation/                     # وحدة الأتمتة
│   ├── __init__.py
│   └── auto_responder.py           # المهام المجدولة والتنبيهات
│
└── dashboard/                      # تطبيق الويب
    ├── app.py                      # تطبيق Flask الرئيسي
    ├── auth.py                     # ✨ نظام المصادقة
    ├── templates/                  # قوالب HTML
    │   ├── index.html              # لوحة التحكم الرئيسية
    │   ├── login.html              # ✨ صفحة تسجيل الدخول
    │   ├── register.html           # ✨ صفحة التسجيل
    │   ├── packets.html            # ✨ صفحة تحليل الباكتات
    │   ├── topology.html           # ✨ صفحة توبولوجيا الشبكات
    │   ├── cloud.html              # ✨ صفحة الخدمات السحابية
    │   └── gns3.html               # ✨ صفحة GNS3 Simulator
    └── static/                     # ملفات ثابتة
        ├── css/
        │   └── style.css           # أنماط مخصصة
        └── js/
            └── dashboard.js        # منطق الواجهة
✨ = ميزة جديدة أو محسّنة

🚀 دليل التثبيت السريع
المتطلبات الأساسية
bash# نظام التشغيل
Ubuntu 20.04+ أو Debian 11+ (موصى به)

# Python
Python 3.11 أو أحدث

# أدوات إضافية
- nmap (للفحص الأمني)
- tcpdump (لتحليل الباكتات - اختياري)
- Docker (لمراقبة الحاويات - اختياري)
- GNS3 Server (للمحاكاة - اختياري)
خطوات التثبيت
1. تحديث النظام
bashsudo apt update && sudo apt upgrade -y
2. تثبيت المتطلبات
bash# Python وأدوات التطوير
sudo apt install -y python3.11 python3.11-venv python3-pip

# nmap للفحص الأمني
sudo apt install -y nmap

# tcpdump لتحليل الباكتات (مطلوب لـ Scapy)
sudo apt install -y tcpdump

# Git لنسخ المشروع
sudo apt install -y git
3. نسخ المشروع
bash# إنشاء مجلد
sudo mkdir -p /var/www/sentraos
cd /var/www/sentraos

# نسخ الملفات (إذا كان على Git)
git clone https://github.com/your-username/sentraos.git .

# أو رفع الملفات يدوياً
4. إعداد البيئة الافتراضية
bash# إنشاء بيئة افتراضية
python3.11 -m venv venv

# تفعيل البيئة
source venv/bin/activate

# ترقية pip
pip install --upgrade pip
5. تثبيت المكتبات
bash# تثبيت جميع المكتبات
pip install -r requirements.txt
6. إعداد ملف التكوين
bash# نسخ ملف البيئة النموذجي
cp .env.example .env

# تعديل الإعدادات
nano .env
محتوى ملف .env:
env# إعدادات الخادم
FLASK_HOST=0.0.0.0
FLASK_PORT=8080
FLASK_DEBUG=False
SERVER_IP=72.61.192.175

# الأمان
SESSION_SECRET=غير-هذا-المفتاح-السري-الآن

# قاعدة البيانات
DATABASE_PATH=sentra.db

# المستخدم الافتراضي
DEFAULT_ADMIN_USERNAME=admin
DEFAULT_ADMIN_PASSWORD=admin123
DEFAULT_ADMIN_EMAIL=admin@sentraos.local

# GNS3 (اختياري)
GNS3_SERVER_URL=http://127.0.0.1:3080
GNS3_USERNAME=
GNS3_PASSWORD=
⚠️ مهم جداً: غيّر SESSION_SECRET إلى مفتاح قوي وفريد!
7. تشغيل التطبيق
bash# تشغيل المشروع
python3 main.py
```

#### 8. الوصول للوحة التحكم
```
افتح المتصفح وانتقل إلى:
http://72.61.192.175:8080

بيانات الدخول الافتراضية:
Username: admin
Password: admin123

📊 واجهات API المتاحة
🔐 المصادقة
bashPOST /login              # تسجيل الدخول
POST /register           # إنشاء حساب جديد
GET  /logout             # تسجيل الخروج
📈 مقاييس النظام
bashGET /api/metrics/current                    # المقاييس الحالية
GET /api/metrics/history?type=cpu&limit=20  # سجل المقاييس
🔒 الفحص الأمني
bashPOST /api/security/scan                     # تشغيل فحص أمني
GET  /api/security/scans?limit=10           # سجل الفحوصات
📦 تحليل الباكتات
bashPOST /api/packets/start                     # بدء التقاط الباكتات
POST /api/packets/stop                      # إيقاف الالتقاط
GET  /api/packets/status                    # حالة الالتقاط
GET  /api/packets/recent?limit=50           # أحدث الباكتات
GET  /api/packets/protocols                 # توزيع البروتوكولات
GET  /api/packets/top-talkers?limit=10      # أكثر IPs نشاطاً
GET  /api/packets/suspicious                # النشاطات المشبوهة
GET  /api/packets/analysis                  # تحليل الترافيك
GET  /api/packets/history?limit=10          # سجل الالتقاطات
🌐 توبولوجيا الشبكات
bashGET  /api/topology/devices                  # جميع الأجهزة
POST /api/topology/ping                     # اختبار Ping
POST /api/topology/traceroute               # Traceroute
GET  /api/topology/routing/:device_id       # جدول التوجيه
GET  /api/topology/interfaces/:device_id    # إحصائيات الواجهات
GET  /api/topology/statistics               # إحصائيات الشبكة
☁️ الخدمات السحابية
bashGET /api/cloud/overview                     # نظرة عامة
GET /api/cloud/containers                   # حاويات Docker
GET /api/cloud/images                       # صور Docker
GET /api/cloud/networks                     # شبكات Docker
GET /api/cloud/volumes                      # مساحات Docker
GET /api/cloud/cost                         # تقدير التكلفة
🎮 GNS3 Integration
bashGET  /api/gns3/status                       # حالة الخادم
GET  /api/gns3/projects                     # جميع المشاريع
GET  /api/gns3/projects/:id                 # تفاصيل المشروع
GET  /api/gns3/projects/:id/nodes           # أجهزة المشروع
GET  /api/gns3/projects/:id/links           # روابط المشروع
POST /api/gns3/projects/:id/start           # تشغيل كل الأجهزة
POST /api/gns3/projects/:id/stop            # إيقاف كل الأجهزة
GET  /api/gns3/statistics                   # إحصائيات GNS3
GET  /api/gns3/templates                    # القوالب المتاحة
🔔 التنبيهات
bashGET  /api/alerts?limit=20&severity=high     # التنبيهات
POST /api/alerts/:id/acknowledge            # إقرار تنبيه
🌐 الشبكة
bashGET /api/network/connections                # الاتصالات النشطة
📊 الإحصائيات
bashGET /api/stats                              # إحصائيات اللوحة

🎯 حالات الاستخدام
1. مراقبة البنية التحتية لتكنولوجيا المعلومات

تتبع أداء الخادم في الوقت الفعلي
تلقي تنبيهات عند تجاوز العتبات
تحليل البيانات التاريخية لتخطيط السعة

2. التدقيق الأمني

تقييمات ثغرات منتظمة
كشف المنافذ المفتوحة
مراقبة الامتثال وإعداد التقارير

3. تحليل حركة الشبكة

التقاط وتحليل الباكتات الفورية
كشف محاولات الاختراق (Port Scanning)
تحليل استخدام البروتوكولات

4. محاكاة وتصميم الشبكات

بناء واختبار توبولوجيا الشبكات
اختبار Ping و Traceroute
تحليل جداول التوجيه

5. إدارة البنية السحابية

مراقبة حاويات Docker
تتبع استخدام الموارد
تقدير التكاليف

6. التدريب والتعليم

محاكاة شبكات باستخدام GNS3
تدريس مفاهيم الشبكات
اختبار السيناريوهات


🔒 الاعتبارات الأمنية
تدابير الأمان المُنفَّذة

المصادقة والترخيص

نظام تسجيل دخول آمن
تشفير كلمات المرور باستخدام Werkzeug
إدارة جلسات مستخدمين
حماية جميع الصفحات والـ APIs


التحقق من المدخلات

فحص أهداف الفحص لمنع SSRF
السماح بفحص localhost فقط افتراضياً
تنقية جميع مدخلات المستخدم


متغيرات البيئة

البيانات الحساسة في ملف .env
عدم تضمين المفاتيح في الكود


أمان قاعدة البيانات

SQLAlchemy ORM يمنع SQL Injection
إدارة جلسات آمنة


النشر في الإنتاج

Nginx كوكيل عكسي
تشفير SSL/TLS
قواعد جدار الحماية


صلاحيات تحليل الباكتات

يتطلب Scapy صلاحيات root لالتقاط الباكتات
استخدم بحذر في بيئة الإنتاج




🧪 الاختبار
اختبارات يدوية
bash# 1. اختبار مقاييس النظام
curl http://localhost:8080/api/metrics/current

# 2. اختبار الفحص الأمني
curl -X POST http://localhost:8080/api/security/scan \
  -H "Content-Type: application/json" \
  -d '{"target":"localhost","type":"quick"}'

# 3. اختبار التنبيهات
curl http://localhost:8080/api/alerts?limit=10

# 4. اختبار تحليل الباكتات (بعد تسجيل الدخول)
curl http://localhost:8080/api/packets/status

# 5. اختبار توبولوجيا الشبكات
curl http://localhost:8080/api/topology/devices

# 6. اختبار Docker
curl http://localhost:8080/api/cloud/containers

# 7. اختبار GNS3
curl http://localhost:8080/api/gns3/status

# 8. التحقق من قاعدة البيانات
python3 -c "from models import get_session; session = get_session(); print('✅ Database: OK')"

📚 الدروس المستفادة والمهارات
المهارات التقنية المُظهَرة

تطوير Backend

تصميم وتنفيذ RESTful API
نمذجة قاعدة البيانات مع SQLAlchemy
جدولة المهام غير المتزامنة


إدارة الأنظمة

تكوين خادم Linux
مراقبة العمليات باستخدام psutil
نشر الخدمات مع Systemd


معرفة الأمان

فحص الشبكة باستخدام nmap
تقنيات تقييم الثغرات
تحليل الباكتات باستخدام Scapy


ممارسات DevOps

الأتمتة والمراقبة
أنظمة التسجيل والتنبيه
استراتيجيات النشر


تطوير Frontend

تصميم ويب متجاوب
تصور البيانات الفورية
AJAX والتحديثات غير المتزامنة


محاكاة الشبكات

بناء توبولوجيا شبكات
خوارزميات التوجيه
اختبار الاتصال


تكامل الأنظمة

التكامل مع Docker API
التكامل مع GNS3 API
معالجة REST APIs
